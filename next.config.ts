import type { NextConfig } from "next";

// Note: Turbopack workspace-root warning can be silenced by running
// `next dev` from the dashboard directory directly (not from the monorepo root).
// The project is self-contained at this path:
//   /Users/ashutosh_bajpai/Desktop/Dashboard-Project/dashboard
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
